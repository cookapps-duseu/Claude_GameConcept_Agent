# 프로젝트 지침

## 작업 방식

bash 명령어 실행 시 파일 수정/추가/제거에 대해 별도 확인 없이 즉시 실행한다.

## 임시 파일 정리

디버깅/수정 목적으로 루트에 임시 .py 스크립트를 생성한 경우, 작업 완료 후 즉시 삭제한다.

## 스킬 우선순위

**새 게임 컨셉 생성 요청** (예: "게임 컨셉 만들어줘", "컨셉안 짜줘", "게임 기획해줘", 게임 아이디어/장르/방식을 설명하며 컨셉을 요청하는 경우)이 들어오면:

- `superpowers:brainstorming` **사용 금지**
- `output/` 폴더 파일 탐색 **금지** — 컨텍스트 수집 없이 즉시 `concept-clarifier` 스킬을 실행한다

**단, 사용자가 명시적으로 이전 결과물을 언급하는 경우는 예외다:**
- "이전 컨셉 수정해줘", "방금 만든 거 다시 실행해", "output의 파일 써줘" 등
- 이 경우 `output/` 파일을 읽고 사용자 의도에 맞게 처리한다

이 규칙은 슈퍼파워 기본 동작보다 우선한다.

---

## 기획 구체화 워크플로우

사용자와 기획안의 특정 영역(전투 시스템, 경제 구조, 레벨 디자인 등)을 대화로 구체화할 때:

### 구체화 완료 시점 판단 기준
다음 중 하나라도 해당하면 구체화가 완료된 것으로 판단한다:
- 사용자가 명시적으로 수락 ("좋아", "그렇게 해", "OK", "확정" 등)
- 사용자가 새로운 방향 지시 없이 질문만 남긴 상태에서 답변 완료
- 처음에 구체화하기로 한 항목이 모두 채워진 경우

### 구체화 질문 전 사전 체크 (매 질문마다 적용)

사용자가 구체화 내용을 작성하면, 질문하기 전에 아래 순서로 점검한다:

1. **단계 적합성 확인** — 사용자가 쓴 내용이 현재 구체화하고 있는 영역(예: 전투 시스템, 경제 구조 등)에 해당하는 내용인가?
   - 범위를 벗어난 내용이 섞여 있으면: 해당 부분은 별도 영역으로 분리해 메모해두고, 현재 단계에서는 다루지 않는다고 명시한다.

2. **누락 항목 체크** — 현재 구체화 영역에서 아직 정해지지 않은 항목이 있는가?
   - 있으면: 질문 목록에 추가한다.
   - 없으면: 구체화 완료로 판단한다.

3. **내용 보완** — 사용자가 쓴 내용 중 모호하거나 수치가 없는 항목은 합리적인 기본값을 제안해 확인받는다.
   - 예: "공격력이 높다" → "기본 공격력 15, 강화 시 +3씩 증가 — 이 방향으로 가도 될까요?"

4. **질문 우선순위 정리** — 위 과정 후 남은 질문들을 핵심 → 세부 순으로 정렬해 한 번에 제시한다. 질문은 최대 3개까지만 한 번에 묻는다.

### 완료 시점에 자동으로 수행할 것

**1단계 — 검토 (대화창 출력)**
다음 3가지 축으로 검토 결과를 대화창에 출력한다:
- **완결성**: 구체화하기로 했던 항목이 모두 채워졌는가
- **일관성**: 기존 컨셉(concept.html, session.json)과 충돌하는 내용이 없는가
- **실행 가능성**: 명세대로 실제로 구현 가능한가, 여전히 모호한 항목이 있는가

**2단계 — 저장**
검토 결과 출력 직후, 아래 경로에 결정 로그 MD 파일을 저장한다:
```
output/{날짜}_{게임이름}/{기획영역_영어명}.md
```

파일 내용 포맷:
```markdown
# {게임명} — {기획 영역명} 구체화

_구체화 일시: {날짜}_

## {섹션명}
- {결정 항목}: {값}
  - 이유: {근거 또는 맥락}
```

- `{기획영역_영어명}`은 대화 맥락에서 판단한다 (예: `combat_system`, `progression`, `economy`)
- 저장 대상 폴더는 `output/` 내 가장 최근 날짜 폴더를 사용한다. 없으면 사용자에게 확인한다.
- 저장 완료 후 파일 경로를 대화창에 표시하고 사용자 피드백을 기다린다.
